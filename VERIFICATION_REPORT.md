# تقرير تحقّق مستقلّ — اختبارات وأداء منصّة Dawrha

> **كل رقم أدناه ناتج عن تشغيل فعلي قمتُ به بنفسي على هذه البيئة بتاريخ 2026-08-18** — لا نقل عن تقارير سابقة ولا تقدير نظري.
> البيئة: NestJS (Node v20.11.1) · PostgreSQL 16 `dawrha_db`@127.0.0.1:5432 (أصلي على المضيف) · Redis 7 (حاوية، منشورة على :6379) · Odoo 19 (حاوية) · k6 v2.2.0.
> الغرض: التحقّق العملي من مجموعة الاختبارات، والفحص العميق لخوارزمية إسناد الطلبات/الشاحنات/التوصيل، وقياس الأداء تحت الحِمل.

---

## 1) الاختبارات الوظيفية (Jest) — مقيسة

| ما شُغِّل | الأمر | النتيجة الفعلية |
|---|---|---|
| المجموعة الكاملة + تغطية | `npx jest --coverage` | **90 سويت / 938 اختبار — كلها ناجحة (100%)** · 36.9s |
| موديول الطلبات وحده | `npx jest src/order` | **20 سويت / 174 اختبار — كلها ناجحة** · 32.3s |

**التغطية (من `coverage/clover.xml` لتشغيلي أنا):**

| المقياس | القيمة |
|---|---|
| الأسطر/العبارات (Statements) | **49.90%** — 6760/13548 |
| الفروع (Branches) | **45.08%** — 4591/10185 |
| الدوال (Functions) | **39.35%** — 968/2460 |
| عدد الملفات المقيسة | 407 |

> **الحكم:** أرقام الاختبارات والتغطية في `TEST_REPORT.md` **صحيحة وقابلة للتكرار** — أعدتُ إنتاجها بالضبط.

---

## 2) الفحص العميق لخوارزمية الطلبات (سؤالك الأساسي)

نفّذتُ اختبار سيناريو مخصّصاً يمرّ السلسلة كاملة على الدوال النقيّة الفعلية في الكود
(`truck-score.ts`, `allocation-planner.ts`, `delivery-capacity-planner.ts`) بأرقام واقعية — **7/7 نجحت**.
النتائج أدناه **محسوبة فعلياً** من تشغيل الكود، لا موصوفة.

### 2.1 حساب كمية الطلبية عبر وحدات قياس مختلفة → وزن بالكيلوغرام

القاعدة في الكود (`delivery-trip.service.ts::partWeights`): سطر بوحدة **KG** يزن كميته؛ أي وحدة أخرى =
`الكمية × product.unit_weight_kg`. وزن الوحدة إلزامي للمواد غير الوزنية.

**طلب معمل تجريبي:**

| المادة | الوحدة | الكمية | وزن الوحدة (kg) | الوزن المحسوب |
|---|---|---|---|---|
| زجاجات بلاستيك | PIECE | 10,000 | 0.025 | **250 kg** |
| كرتون | KG | 500 | — | **500 kg** |
| علب ألمنيوم | PIECE | 20,000 | 0.015 | **300 kg** |
| مادة بلا وزن مُسجَّل | PIECE | 100 | (فارغ) | **0 kg** |
| | | | **الإجمالي** | **1050 kg** |

- **ملاحظة مهمة (خطر جودة بيانات):** المادة التي لا يملك مديرها `unit_weight_kg` تُحسب بوزن **صفر** ولا يرمي النظام خطأً — أي أن الحمولة قد تُقدَّر **أخفّ من حقيقتها**، وقد يُختار محرك أصغر مما يجب. الكود يعتبرها "مشكلة بيانات تُصلَح في الأعلى". **التوصية:** فرض `unit_weight_kg > 0` على كل مادة غير وزنية عند الإنشاء (وليس تركها اختيارية).

### 2.2 خوارزمية اختيار الشاحنة (Score) — لحمولة 1050 kg

المبدأ في `truck-score.ts`: قاعدة صلبة واحدة (وجود سائق فعّال يتقدّم على كل شيء)، ثم درجة متوازنة =
`0.5·السعة (أفضل-تطابق، load-aware) + 0.5·العدالة (الأقل استخداماً)`.

| الشاحنة | السعة kg | رحلات حديثة | مكوّن السعة | مكوّن العدالة | **الدرجة** | يسع الحمولة؟ |
|---|---|---|---|---|---|---|
| #12 | 1200 | 0 | 0.9375 | 1.0 | **0.9688 ← الفائزة** | ✅ |
| #11 | 5000 | 0 | 0.605 | 1.0 | 0.8025 | ✅ |
| #14 | 800 | 0 | 0.381 | 1.0 | 0.6905 | ❌ |
| #13 | 1200 | 9 | 0.9375 | 0.0 | 0.4688 | ✅ |

**الاستنتاجات المقيسة:**
1. **أفضل-تطابق يعمل:** الشاحنة 1200 (#12) هزمت الـ5000 (#11) — النظام لا يحجز شاحنة عملاقة لحمولة صغيرة.
2. **العدالة تعمل:** الشاحنة المرهَقة (#13، 9 رحلات) هبطت للقاع رغم أنها تسع الحمولة.
3. **`fits` ليست مرشّحاً صلباً:** شاحنة صغيرة طازجة (#14، لا تسع) سبقت شاحنة تسع لكن مرهَقة (#13). **هذا مقصود** (نموذج الـ milk-run يسكب الفائض على شاحنة ثانية) — لكنه يعني احتمال رحلة إضافية. سليم منطقياً، ويستحق الانتباه تشغيلياً.

### 2.3 اختيار المستودعات (Allocation)

القاعدة في `allocation-planner.ts`: (1) مستودع واحد يغطّي الكل = الأقرب من بين المُغطّين، ويُفضَّل **دائماً** على أي تقسيم؛ (2) وإلا أقل عدد مستودعات يغطّي؛ (3) وبين المتساوية الأقلّ مسافةً؛ (4) وإلا أفضل تغطية جزئية تُعرَض على المشتري.

| السيناريو | الناتج الفعلي | ملاحظة |
|---|---|---|
| مستودع بعيد (30كم) يملك كل الأسطر، مقابل تقسيم قريب (5+12=17كم) | اختار **الواحد البعيد (30كم)** | **ملاحظة تقييمية** ⚠️ |
| لا مستودع واحد يغطّي؛ قريب(5)+وسط(12) معاً يغطّيان | اختار **التقسيم {5+12}=17كم** | صحيح (أقل عدد ثم أقرب) |

- **الملاحظة ⚠️:** قاعدة "مستودع واحد يفوز دائماً" **عمياء عن المسافة** مقابل التقسيم — فقد تختار مستودعاً واحداً **أبعد وأغلى في التوصيل** بدل تقسيم أقرب بكثير. هذا **قرار عمل مقصود وموثّق** (مدير واحد/رحلة واحدة/فاتورة واحدة)، لكنه يزيد كلفة/مسافة التوصيل في بعض الحالات. **التوصية:** إضافة عتبة (مثلاً: فضّل التقسيم إذا كان أقصر بنسبة X% وأقلّ كلفة توصيل من الواحد).

### 2.4 تقسيم رحلات التوصيل على الشاحنات بالوزن (`planDeliveryTrips`)

نموذج الـ milk-run: تنطلق الشاحنة من **أبعد مستودع** وتتّجه للمشتري وتلتقط في الطريق حتى تمتلئ.

| الحمولة | سعة الشاحنة | الناتج الفعلي |
|---|---|---|
| جزء 700kg (قريب 5كم) + جزء 600kg (بعيد 30كم) | 1000kg | **رحلتان** — الأولى تبدأ من **البعيد (30كم)** |
| نفس الجزأين (1300kg إجمالاً) | 1500kg | **رحلة واحدة** تحمل 1300kg، تبدأ من البعيد |
| جزء واحد 4200kg | 3000kg | رحلة واحدة مع علم **`overCapacity=true`** (لا يُخفى) |

سليم ومتّسق: الوزن يقرّر عدد الرحلات، والفائض عن السعة يُطلق رحلة تالية، والجزء الأثقل من أي شاحنة يُرفع كتنبيه بدل إخفائه.

---

## 3) الأداء والحِمل (k6) — مقيسة على البيئة الحيّة

الإعداد: 30 VUs (ذروة) · تصاعد 10s→ثبات 20s→هبوط 10s (≈40s) · 3 مسارات ضيف حقيقية تنفّذ استعلامات
`EXISTS` مترابطة على التسعير: `/api/v1/user-app/guest/{categories,products,offers}`.

### أ) قاعدة فارغة + محدِّد المعدّل فعّال
| المقياس | القيمة الفعلية |
|---|---|
| إجمالي الطلبات (40s) | **64,716** → **1,617 طلب/ثانية** واردة |
| زمن الطلبات الناجحة (200) | p50 **6.9ms** · p95 **10.7ms** · max 66ms |

### ب) قاعدة مليئة (8 تصنيفات · 96 مادة · 384 صف تسعير) + محدِّد فعّال
| المقياس | القيمة الفعلية |
|---|---|
| زمن الطلبات الناجحة (غالباً كاش) | p50 **7.6ms** · p95 **15.2ms** · max 170ms |
| ناجحة (200) / مرفوضة (429) | 298 / 64,639 |

### ج) السعة الخام — رُفع المحدِّد مؤقتاً ثم أُعيد (قاعدة مليئة)
| المقياس | القيمة الفعلية |
|---|---|
| إجمالي الطلبات المُخدَّمة (200) | **26,809** — **0 فشل (0.00%)** |
| **الإنتاجية الفعلية** | **670 طلب/ثانية** مُخدَّمة |
| زمن الاستجابة | avg 33ms · p50 **33ms** · p90 50ms · p95 **56ms** · max 250ms |

### حقيقة مهمة عن محدِّد المعدّل (صُحِّح فهمها بالقياس)
المحدِّد يُبوّب **لكل (مسار + IP)** وليس لكل IP فقط: كل مسار له سلّة **100 طلب / 60 ثانية**، فيمرّ من IP
واحد يطرق 3 مسارات ≈ **300 طلب مخدوم/نافذة**، ويُرفض الباقي بـ429. تأكّد ذلك بالقياس (298 ناجحة عبر 3 مسارات).
**الخلاصة:** المحدِّد يعمل بفعالية ويحمي الخادم؛ والنسخة الواحدة تخدم **~670 طلب/ثانية بلا فشل** عند 30 مستخدماً متزامناً وزمن p95≈56ms.

### تحذير دقّة صادق
بيانات الكتالوج التي أدرجتُها **اصطناعية** (96 مادة) لغرض القياس؛ قاعدة إنتاج بعشرات الآلاف من الأسطر قد ترفع أزمنة الـDB قليلاً. الأرقام أعلاه لنسخة تطبيق واحدة على جهاز واحد.

---

## 4) المشاكل والملاحظات المكتشفة أثناء التنفيذ (حقيقية)

| # | الخطورة | الوصف | الأثر | التوصية |
|---|---|---|---|---|
| 1 | **مرتفعة** | `npm run seed` **يفشل**: `condition-seed.ts` يزرع شروطاً عامة بلا `product_id`، لكن `material_conditions` صار يتطلّب `product_id` (NOT NULL + `@Unique(productId,code)`) | بيئة جديدة لا تُزرَع بالكامل بالأمر الموثّق (تتوقّف بعد الوحدات) | تحديث `condition-seed` ليزرع الشروط لكل منتج، أو جعل الشروط الافتراضية عامة في المخطط مجدداً |
| 2 | متوسطة | سكربت `start:prod` = `node dist/main` بينما البناء يُخرج `dist/src/main.js` | `npm run start:prod` يفشل بـ`MODULE_NOT_FOUND` | تصحيح السكربت إلى `node dist/src/main` (وكذلك `start:worker`) |
| 3 | متوسطة | تفضيل "مستودع واحد" أعمى عن المسافة مقابل التقسيم (§2.3) | قد يختار توصيلاً أبعد/أغلى | عتبة تفضّل التقسيم عند فارق مسافة/كلفة كبير |
| 4 | متوسطة | `unit_weight_kg` المفقود يُحسب 0 في وزن الشاحنة (§2.1) | تقدير حمولة أخفّ من الحقيقة → شاحنة أصغر مما يجب | فرض وزن وحدة > 0 لكل مادة غير وزنية |
| 5 | منخفضة (بالتصميم) | `fits` ليست مرشّحاً صلباً في اختيار الشاحنة (§2.2) | احتمال رحلة إضافية | مقبول ضمن نموذج milk-run؛ للتوثيق فقط |
| 6 | منخفضة | لا `helmet` ولا CORS صريح (مذكور في `NFR_REPORT.md`) | ترويسات أمان ناقصة | إضافتهما في `main.ts` |

---

## 5) ما لم يُختبَر حيّاً (حدود صريحة)
- **دورة طلب كاملة end-to-end حيّة** (سلة→checkout→حجز Odoo→فرز→توصيل): تتطلّب مصادقة + مستودعات ومخزون وشاحنات مزروعة + مسار كتابة Odoo. تحقّقتُ من **منطقها** عبر 174 اختبار وحدة للطلبات + اختبار السيناريو النقيّ في §2. لم أُنفّذ طلباً حيّاً واحداً كاملاً.
- **اختبارات الأودو:** شُغِّلت على قاعدة اختبار منفصلة `odoo_test_run` (لعدم المساس بـ`odoo19` الحيّة، ثم حُذفت). النتائج في §7.

---

## 7) اختبارات الأودو (addon `recycle_warehouse`) — مقيسة

الأمر: `odoo -d odoo_test_run -i recycle_warehouse --test-enable --test-tags=/recycle_warehouse --stop-after-init` داخل حاوية `odoo19`.

| المقياس | القيمة الفعلية |
|---|---|
| أصناف الاختبار المُنفَّذة | **25 صنفاً** |
| دوال الاختبار المُنفَّذة | **202 دالة** |
| ناجحة | **199** |
| إخفاقات (FAIL) | **1** |
| أخطاء (ERROR) | **2** |
| أصناف لم تُنفَّذ | ~6 (توقّف عند `test_report_arabic`) |

**الإخفاقات الثلاثة (بأسبابها الفعلية من السجلّ):**
1. `TestBackendAutoSync.setUpClass` → **ERROR حقيقي في المخطط** (وليس بيئياً كما ذُكر سابقاً):
   `null value in column "uom_id" of relation "recycle_product" violates not-null constraint` —
   الاختبار ينشئ `recycle.product` بلا `uom_id` بينما العمود صار **NOT NULL**. **نفس نمط عطل `npm run seed`** في الباك يند: انحراف بين كود الاختبار/الزرع والمخطط الحالي.
2. `TestProductSuggestion.test_cannot_submit_twice` → **FAIL**.
3. `TestProductSuggestion.test_payload_carries_the_unit_code_not_its_label` → **ERROR**.

**التوقّف عند `test_report_arabic`:** يستدعي `wkhtmltopdf` لتوليد PDF عربي، ويتعلّق في الحاوية بلا واجهة رسومية
(تأكّدت من تجمّد السجلّ عند استدعاء الأداة). **قيد بيئي متكرّر، ليس عيب كود** — لذلك لم تُنفَّذ الأصناف الستّة الأخيرة.
أوقفتُ العملية العالقة يدوياً وحذفتُ قاعدة الاختبار.

> **الحكم:** الأغلبية الساحقة من اختبارات الأودو تمرّ (199/202 مُنفَّذة). الإخفاقان في `TestProductSuggestion`
> وخطأ `TestBackendAutoSync` (المخطط) **يستحقّون الإصلاح**؛ توقّف الـPDF بيئيّ. ادّعاء `TEST_REPORT.md` بأن
> خطأ `TestBackendAutoSync` "بيئي (يتطلّب NestJS حيّ)" **غير دقيق** — سببه الفعلي قيد `uom_id NOT NULL`.

---

## 6) حالة البيئة بعد التشغيل (لعلمك)
- أدرجتُ كتالوجاً **اصطناعياً** في `dawrha_db` (8 تصنيفات/96 مادة/384 تسعير) لأجل قياس الحِمل — أخبرني إن رغبت بحذفه.
- شغّلتُ حاوية Redis جديدة `dawrha-redis` منشورة على :6379 (لم تكن منشورة).
- ضبطتُ في الأودو `recycle.backend_base_url` + `recycle.backend_webhook_secret` (كانا مفقودَين — سبب توقّف المزامنة).

---

## 7) الإصلاحات والتحسينات المنفَّذة في هذه الجلسة (مع دليل الاختبار الحيّ)

> **الحالة النهائية: مجموعة الباك يند الكاملة 944/944 اختبار تمرّ · ترقية موديول الأودو نظيفة · الهجرة مطبَّقة.**
> كل بند أدناه **نُفِّذ في الكود واختُبِر** (حيّاً أو باختبار وحدة)، لا ادّعاء بلا تحقّق.

### أ) عيوب أساسية أُصلحت
| العيب | الجذر | الإصلاح | الدليل |
|---|---|---|---|
| `npm run seed` يفشل | `condition-seed` يزرع شروطاً عامة بينما `material_conditions.product_id` صار NOT NULL | إزالة الزرع العام (الشروط per-product بالتصميم) | ✅ الـseed اكتمل |
| `npm run start:prod` يفشل | يشير `dist/main` بدل `dist/src/main` | تصحيح المسار | ✅ يعمل |
| إنشاء أدمن أودو ثانٍ يفشل | أودو 19 غيّر `res.users.groups_id` → `group_ids` | تصحيح اسم الحقل | ✅ HTTP رجع `success` (odoo user id=9, is_system=True) |
| `limit=10` لا يُظهر تصنيفات | كاش يخزّن **نتيجة فارغة** تحت الحدّ الافتراضي ولا يُبطَل من مسار خارج الـAPI | حارس "لا تُخزّن نتيجة فارغة" في guest + catalog.service | ✅ limit=10 صار 8 تصنيفات؛ البحث الفارغ لا يُخزَّن |

### ب) الترقيم (limit & page) — رقم موجب لا نصّ
- **الـDTO الأساسي `PaginationQueryDto`** يفرض `@Type(()=>Number) @IsInt() @Min(1)` (والحدّ `@Max(50/100)`) — أي **رقم موجب دائماً**. كل الـDTOs المشتقّة (كتالوج/ضيف/طلبات/محفظة) ترثه أو تكرّره بنفس التحقّق.
- المخالف الوحيد كان `truck-tracking history` يقرأ `@Query('limit') limit: string` خام → **أُصلح** بـDTO مُتحقَّق (`@Type Number, @Min 1, @Max 500`).

### ج) المزامنة بين النظامين — التشخيص والتقوية (سؤالك الأساسي)
**قصة المزامنة:** نموذج الأودو عند create/write → **ping موقّع بعد الـcommit** → webhook الباك يند (202) → مهمة طابور تسحب الحالة كاملة من الأودو عبر JSON-RPC → مرآة/تبنّي. + **كرونات مصالحة** (كل 10د، في عملية الـworker) كشبكة أمان.

**السبب الجذري الوحيد لكل أعطال المزامنة** (شاحنات، مستودعات، اقتراحات، منتجات): كان `recycle.backend_base_url` + `recycle.backend_webhook_secret` **غير مضبوطَين** في الأودو → **كل** ping صادر يُسقَط **بصمت** (على مستوى DEBUG). إصلاحات التقوية:
1. ضبط المفتاحين (حيّاً).
2. **جعل الفشل مرئياً** (DEBUG → WARNING برسالة عمليّة).
3. **واجهة إعدادات في الأودو** (Settings → Recycling): رابط الباك يند + السرّ + العتبات (لم يكن لها مكان مكتشَف من قبل).
4. **زرّ "Test backend connection"** يُرسل ping حقيقياً ويُظهر النتيجة → الفشل الصامت صار مرئياً بضغطة.
5. **`extra_hosts: host.docker.internal:host-gateway`** في docker-compose ليصل الأودو (حاوية) للباك يند (مضيف).

**الدليل الحيّ:** مستودع أُنشئ في الأودو → webhook 202 → **الباك يند تبنّاه** (`odoo_warehouse_id:1`). شاحنة جمع → ظهرت؛ شاحنة توصيل → بقيت في الأودو. زرّ الاختبار → `success (HTTP 202)`.

### د) مزامنة المنتج والشاحنة (باك يند ↔ أودو)
- **منتج (إنشاء/تعديل/حذف):** يُدفع للأودو تلقائياً. أصلحتُ التعديل ليدفع **الاسم + الوحدة + التصنيف** (كان الاسم فقط) + `write` في الأودو يعالج `unit_code`. **دليل حيّ:** تعديل اسم منتج في الباك يند → أودو `recycle_product` تحدّث لـ`EDITED-LIVE`. (المنتجات تُؤلَّف في الباك يند بالتصميم؛ الأودو يقترح فقط — لا مسار عكسي متضارب.)
- **شاحنة الجمع:** تُزامَن للباك يند؛ **التوصيل تبقى في الأودو**. أضفتُ **الطول/العرض** (اختياريين) في الأودو ومرآتهما في الباك يند.

### هـ) اللغة والتوكن (#14)
- **`deviceId` في التوكن** (access/refresh) → معرفة من أي جهاز. **دليل:** فكّ التوكن أظهر `deviceId`.
- **لغة الحساب تُطبَّق بلا هيدر** وتفوز على الهيدر (interceptor يقرأ `req.user.language`). **دليل حيّ:** لغة الحساب=عربي وبلا هيدر → الرد **عربي**.

### و) الإشعارات (#17)
- محتوى الإشعار يُترجَم **عند العرض** بلغة القارئ من `metadata.i18n` (كان الـpush فقط مترجَماً). **دليل:** 5/5 اختبارات (منها اختباران جديدان للترجمة والـfallback).

### ز) التسعير — الانتهاء والحذف (#20)
- **الأرشفة التلقائية عند الانتهاء**: موجودة أصلاً (`sweepExpiredPricing` عبر كرون كل 5د → يؤرشف بـ`EXPIRED` ثم **يحذف من الحالية** + يُشعِر الأدمن). مغطّاة باختبار.
- **استهداف أدوار متعددة (+ الكل)** للانتهاء والحذف: أضفتُ `tiers[]` (فارغ=الكل). **دليل حيّ:** انتهاء بـ`["INDIVIDUAL"]` → **INDIVIDUAL فقط** أخذ `effective_until`، والبقية لا.

### ح) حذف الحالة/المادة (#21)
- حذف **حالة عليها سعر** → **مرفوض** ("remove it from the price list first") — موجود.
- حذف **مادة عليها تسعير حيّ** → كان يفشل بخطأ FK خام (`RESTRICT`)؛ أضفتُ **حارساً نظيفاً مترجَماً** ("withdraw its pricing first, then delete it").
- (ملاحظة: `product_pricing_history → products` هو CASCADE — حذف المادة يمحو أرشيف تسعيرها؛ للنقاش إن أردت الاحتفاظ به.)

### ط) الشكاوى على الطلبية كاملة (#23)
- كانت **على مستوى الجزء** (`part_id` إلزامي، الروت `/parts/:partId/complaints`). حوّلتها لتكون على **الطلبية كاملة**: هجرة (`part_id`/`warehouse_id` → nullable)، روت **`POST /orders/:orderId/complaints`**، وشكوى المستودع **تُعمَّم على كل مستودعات الطلبية**. **دليل:** الروت مُركَّب (401) + 4 اختبارات وحدة (تعميم على مستودعَين، رفض قبل التسليم).

### ي) واجهة رحلات التوصيل في الأودو (#22)
- الواجهة **موجودة** (نموذج `recycle.delivery.trip` + قائمة/فورم يعرضان المسار: المسافة، المحطات لكل مستودع مع روابط خرائط، الشاحنة، فلتر **"Live"** للنشطة). أضفتُ **فلترة/تجميع بالمستودع والشاحنة** (لمطابقة "بعد تحديد المستودعات").

---

## 8) نقاط للنقاش (قرارات معمارية، ليست أعطالاً)
- **"مزامنة عكسية" للمنتجات (أودو→باك يند):** المنتجات تُؤلَّف في الباك يند بالتصميم؛ لم أبنِ مساراً عكسياً لأنه يخلق مصدرَي حقيقة متضاربَين. أصلحتُ الاتجاه المطلوب بالكامل.
- **وصول الأودو→المضيف:** `extra_hosts` أُضيف لكن تفعيله يحتاج `docker compose up -d` (إعادة إنشاء الحاوية)، وقد يحتاج جدار حماية ويندوز السماح للحاوية. استدعاء الباك يند→أودو يعمل الآن.
- **أرشيف تسعير مادة محذوفة:** يُمحى حالياً (CASCADE). إن أردت الاحتفاظ به للأودِت، نغيّر الـFK.

---

## 9) الدفعة الأخيرة — نتائج الاختبار الحقيقية النهائية وإضافات

> **قياس نهائي (تشغيل فعلي):** مجموعة الوحدة **944 / 944 اختبار تمرّ** (90 سويت · ~19s) · **e2e 70 / 70 اختبار تمرّ** (8 سويت · ~14s) · ترقية موديول الأودو نظيفة · هجرتان جديدتان مطبَّقتان.

### أ) المزامنة العكسية (أودو → باك يند) — منفَّذة ومُختبَرة حيّاً
- **قبل:** تعديل اسم منتج **في الأودو** لا يصل الباك يند (المنتجات كانت اتجاهاً واحداً بالتصميم).
- **الآن:** `recycle.product.write` عند تغيّر الاسم → ping موقّع → webhook باك يند `POST /odoo/webhooks/products` → مهمة `SYNC_PRODUCT_FROM_ODOO` تقرأ الاسم من الأودو وتعكسه.
- **كاسر الحلقة (مهم):** الباك يند يكتب **فقط عند اختلاف فعلي** و**لا يُعيد الدفع** أبداً؛ فالدفع (باك يند→أودو) والمرآة (أودو→باك يند) يتقاربان بعد قفزة واحدة بلا ping-pong.
- **دليل حيّ:** غيّرتُ الاسم في الأودو إلى `RENAMED-IN-ODOO` → **الباك يند عكسه** (كان `Plastic grade EDITED-LIVE`). ✅

### ب) إعادة إرسال رمز نسيت كلمة المرور — موجودة أصلاً
- الرواتات الأربعة `POST /auth/password/forgot/{user-app,collector-app,factory-app,admin}/resend` **مُنفَّذة** بنفس قصة الرايت لميت كإعادة إرسال تأكيد الإيميل: `@Throttle(5/60s)` + cooldown (429 مع `remainingSeconds`) + حدّ يومي (`OTP_MAX_DAILY_RESENDS`). **دليل حيّ:** الروت يستجيب (404 لإيميل مجهول). لا حاجة لكود جديد.

### ج) تسمية موقع المستخدم — منفَّذة (اسم فريد لكل مستخدم)
- عند إضافة موقع في **حساب المستخدم فقط** (بلا مساس بالـonboarding): حقل `name` اختياري ("منزلي"/"عملي") عبر DTO خاص `AddLocationDto` يمتدّ الـLocationDto المشترَك.
- **الفرادة لكل مستخدم** (لا عالمياً): فحص في الخدمة + **فهرس فريد جزئي** `(cititzen_profile_id, lower(name)) WHERE name IS NOT NULL` (هجرة مطبَّقة). فمستخدمان يحقّ لهما "منزلي"، لكن مستخدماً واحداً لا يملك اثنين.

### د) استهداف أدوار انتهاء التسعير + الأرشفة — مُختبَرة حيّاً
- **دليل حيّ:** انتهاء بـ`tiers:["INDIVIDUAL"]` → **INDIVIDUAL فقط** أخذ `effective_until` (COMPANY/FACTORY/FREE_FACILITY لا). ✅
- الأرشفة عند مرور الوقت (`sweepExpiredPricing`: يؤرشف بـ`EXPIRED` ثم يحذف من الحالية) موجودة ومغطّاة باختبار.

### هـ) تقوية المزامنة (خلاصة عملية — ماذا تفعل عند فشل الاتصال)
النظام الآن يتعافى تلقائياً بثلاث طبقات:
1. **الطابور (BullMQ):** كل دفعة للأودو مهمة بإعادة محاولة + backoff (2–3 محاولات)؛ فشل الاتصال العابر يُعاد تلقائياً.
2. **كرونات المصالحة:** (fleet/warehouse/catalog-push... كل 10د في الـworker) تسحب/تدفع الحالة كاملة دورياً — فحتى لو ضاع ping، تُصحَّح الحالة خلال دقائق. **شرط:** تشغيل عملية الـworker (`npm run start:worker`).
3. **الرؤية:** فشل الإعداد صار WARNING + **زرّ "Test backend connection"** في إعدادات الأودو يكشف العطل فوراً.
**عند فشل اتصال أثناء عملية:** لا تُفقَد العملية — تبقى في الطابور/تُلتقط بالمصالحة؛ راجع سجلّ WARNING أو اضغط زرّ الاختبار لتشخيص الرابط/السرّ/الشبكة.
