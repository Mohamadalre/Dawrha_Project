# تقرير الاختبارات — منصّة Dawrha

> **كل رقم أدناه مقيسٌ من تشغيل فعلي على هذه البيئة** — لا تقديرات ولا نظريات. تاريخ القياس: 2026-08-19.
> **البيئة:** Backend (NestJS 11 / Node v20.11.1) · PostgreSQL 16 `dawrha_db`@127.0.0.1:5432 · Redis 7@:6379 · Odoo 19 (حاوية Docker، addon `recycle_warehouse`) · k6 v2.2.0.
> يُقرأ مع `NFR_REPORT.md` (المتطلبات غير الوظيفية) و`VERIFICATION_REPORT.md` (تفاصيل قياس الحِمل والخوارزمية).

---

## 1) منهجية الاختبار (طبقات)

| الطبقة | الأداة | ما تقيسه | ضدّ ماذا |
|---|---|---|---|
| Unit | Jest | منطق الخدمات/الكنترولرات معزولاً | مستودعات وهمية (mocks) |
| E2E (تعاقد) | Jest + Supertest عبر Nest `TestingModule` | الكنترولر + ValidationPipe + الحمايات + مغلّف الاستجابة | مستودعات وهمية |
| **E2E (نظام حيّ)** | **سكربت Node يقود HTTP حقيقي + تحقّق قاعدة** | **رحلة مستخدم كاملة عبر النظامين** | **باك يند + PostgreSQL + Redis + Odoo حيّة (بلا mocks)** |
| Coverage | Istanbul (clover) | نسبة تغطية الأسطر/الفروع/الدوال | — |
| Load/Perf | k6 | الإنتاجية وزمن الاستجابة تحت حِمل | خوادم حيّة |
| Unit (Odoo) | إطار Odoo `TransactionCase` | منطق الأودو | قاعدة مؤقّتة تُنشأ وتُحذف |

**حجم بيانات الاختبار:**
- اختبارات الوحدة/التعاقد: تجهيزات صغيرة حتمية (١–٢٠ صفّاً لكل اختبار) تُبنى وتُلغى داخل كل اختبار.
- اختبار النظام الحيّ وقياس الحِمل: **قاعدة مملوءة فعلياً** — الأحجام في §6.

---

## 2) نتائج Backend — Unit + E2E (تعاقد) + Coverage — مقيسة

| المقياس | القيمة الحقيقية |
|---|---|
| سويتات الوحدة | **90** |
| اختبارات الوحدة | **944 / 944 ناجحة (100%)** |
| زمن تشغيل الوحدة | ~20–37 ثانية (أطول مع أداة التغطية) |
| سويتات/اختبارات E2E (تعاقد) | **8 / 70 ناجحة (100%)** |
| التغطية — الأسطر (Statements) | **49.94%** (6828/13672) |
| التغطية — الفروع (Branches) | **45.15%** (4674/10353) |
| التغطية — الدوال (Functions) | **39.46%** (979/2481) |
| عدد ملفات المصدر المقيسة | 408 |

**ملفات E2E (تعاقد) الثمانية:** account-status-scope · delivery-rate · driver-handover · google-account-signin · guest-app-routes · odoo-order-webhook · onboarding-response-shape · response-envelope.

---

## 3) ⭐ اختبار النظام الحيّ End-to-End (عبر النظامين — بلا mocks)

سكربت (`e2e-live.mjs`) يقود النظام الحيّ الكامل عبر HTTP حقيقي ويتحقّق من الأثر في قاعدة البيانات؛ لكل خطوة زمنها الفعلي.
**النتيجة: 13/13 ناجحة.**

| # | السيناريو | النتيجة | الزمن الفعلي |
|---|---|---|---|
| 1 | إقلاع الـAPI + ترويسات Helmet الأمنية موجودة (HSTS/nosniff/X-Frame/Referrer، وحذف X-Powered-By) | ✅ | 55.1 ms |
| 2 | دخول الأدمن يُرجع JWT يحمل `deviceId` في الحِمل | ✅ | 337.1 ms |
| 3 | كلمة مرور خاطئة تُرفَض (400) | ✅ | 11.5 ms |
| 4 | كتالوج الضيف: مغلّف صحيح + عدد المنتجات (8 تصنيفات) | ✅ | 34.5 ms |
| 5 | **الترقيم: `limit=10` يُرجع بيانات** (اختبار انحدار لبُغّ الكاش الفارغ المُصلَح) | ✅ | 13.9 ms |
| 6 | الترقيم: `limit=abc` يُرفَض 400 (تحقّق رقم موجب) | ✅ | 8.7 ms |
| 7 | i18n: `x-lang=ar` يُرجع رسالة عربية | ✅ | 7.8 ms |
| 8 | الكتالوج المُصادَق: التصنيف أوبجكت + `has_offer` + **لا حالة لشريحة INDIVIDUAL** + السعر لشريحته | ✅ | 26.7 ms |
| 9 | روت الأدمن: الحسابات المؤرشفة (رسالة مترجمة) | ✅ | 23.1 ms |
| 10 | webhook الأودو يرفض سرّاً خاطئاً (403) | ✅ | 10.6 ms |
| 11 | webhook الأودو يقبل السرّ الصحيح (202) | ✅ | 10.4 ms |
| 12 | **Backend→Odoo:** الكتالوج مُزامَن (95/95 منتج يحمل `odooProductId`) | ✅ | 212.5 ms |
| 13 | **⭐ Odoo→Backend (مزامنة آلية):** مستودع أُنشئ في الأودو **ظهر تلقائياً في الباك يند خلال ~1.5s بلا أي webhook يدوي** | ✅ | 7224 ms\* |

\* الزمن الإجمالي يشمل إنشاء المستودع داخل حاوية الأودو + انتظار السلسلة (postcommit ping → `host.docker.internal:3000` → webhook → مهمة مزامنة → تبنّي). **زمن الظهور الفعلي في الباك يند ≈ 1.5 ثانية.**

> **الأهمية:** الخطوة 13 تُثبت أن **السلسلة الكاملة للمزامنة الآلية عبر النظامين تعمل end-to-end** بعد ضبط الإعداد و`extra_hosts` — وهي جوهر تكامل المشروع.

---

## 4) نتائج Odoo — Unit (مقيسة، قاعدة مؤقّتة معزولة)

| المقياس | القيمة الحقيقية |
|---|---|
| أصناف الاختبار المُنفَّذة | **25** |
| دوال الاختبار المُنفَّذة | **202** |
| ناجحة | **199** |
| أصناف لم تُنفَّذ | ~6 — توقّف بيئي عند `test_report_arabic` (wkhtmltopdf في حاوية بلا واجهة — ليس عيب كود) |

**تصحيح هذه الجولة:** الإخفاقان القديمان في `TestProductSuggestion` (`test_cannot_submit_twice` و`test_payload_carries_the_unit_code`) كانا يختبران سلوكاً **ملغى** (الوحدة لم تعد تُرسَل، والإرسال صار idempotent)؛ **حُدِّثا ليطابقا السلوك المقصود**، فصار الصنف كاملاً **6/6 ناجحة** (مُثبَت بتشغيل معزول). يبقى خطأ `TestBackendAutoSync.setUpClass` (قيد `uom_id NOT NULL` في مُجهِّز الاختبار) للمتابعة.

---

## 5) اختبار الحِمل والأداء — k6 (مقيسة)

الإعداد: 30 VUs (ذروة) · ≈40s · 3 مسارات ضيف حقيقية تنفّذ استعلامات `EXISTS` على التسعير.

| السيناريو | إجمالي الطلبات | الإنتاجية | زمن الطلبات الناجحة (p50 / p95) |
|---|---|---|---|
| قاعدة فارغة + محدّد | 64,716 | 1,617 req/s وارد | 6.9 ms / 10.7 ms |
| قاعدة مليئة + محدّد (كاش) | ~65,000 | — | 7.6 ms / 15.2 ms |
| **السعة الخام (رُفع المحدّد ثم أُعيد)** | **26,809 — 0 فشل** | **670 req/s مُخدَّم** | **33 ms / 56 ms** |

**استنتاج أمني مقيس:** محدّد المعدّل يُبوّب لكل (IP+مسار) 100/60s، فرفض عشرات الآلاف من الطلبات بـ429 وحمى الخادم؛ والنسخة الواحدة خدمت **670 طلباً/ثانية بلا فشل (0.00%)** عند 30 مستخدماً متزامناً. التفاصيل والتحذيرات في `VERIFICATION_REPORT.md`.

---

## 6) حجم البيانات المستخدمة في القياس (دقيق — من القاعدة الحيّة)

| الجدول | عدد الصفوف |
|---|---|
| provinces (المحافظات) | 14 |
| waste_categories (التصنيفات) | 8 |
| products (المواد) | 95 |
| product_pricing (صفوف التسعير) | 380 (95 مادة × 4 شرائح) |
| measurement_units (وحدات القياس) | 2 (KG / PIECE) |
| warehouses (المستودعات) | 2 |
| trucks (الشاحنات) | 1 |
| accounts (الحسابات) | 2 |

> البيانات المُدرجة **اصطناعية** لغرض القياس؛ قاعدة إنتاج بعشرات الآلاف قد ترفع أزمنة الـDB قليلاً (تحذير دقّة صادق).

---

## 7) تقييم الاختبارات (رأي مهني)

- **التغطية الوظيفية قوية على منطق الأعمال:** 944 اختبار وحدة تغطّي الكتالوج/الطلبات/الخوارزمية/المصادقة/المزامنة؛ وخوارزمية إسناد الطلبات والشاحنات مُتحقَّقة بأرقام محسوبة فعلياً (`VERIFICATION_REPORT.md`).
- **أُضيف اختبار نظام حيّ end-to-end** يسدّ فجوة كانت قائمة (سويتات E2E السابقة تستخدم mocks): يختبر الآن طبقة القاعدة والتكامل الحيّ مع الأودو فعلياً، بما فيه **المزامنة الآلية عبر النظامين**.
- **نقاط للتحسين مستقبلاً:** رفع تغطية الأسطر فوق 50% (التركيز الحالي على المنطق أكثر من المسارات النادرة)؛ توسيع E2E الحيّ ليشمل دورة طلب كاملة (checkout → حجز Odoo → فرز → توصيل) عند تجهيز حساب معمل مكتمل بمخزون.
- **الأودو:** الأغلبية تمرّ؛ إخفاق `TestBackendAutoSync` سببه قيد `uom_id NOT NULL` (يستحق إصلاح المُجهِّز)، وتوقّف PDF بيئيّ لا برمجيّ.

---

## 8) كيفية إعادة التشغيل (للجنة التقييم)
```bash
# اختبارات الوحدة + التغطية
npx jest --coverage
# اختبارات E2E (التعاقد)
npx jest --config ./test/jest-e2e.json
# اختبار النظام الحيّ (يتطلّب الباك يند + PostgreSQL + Redis + Odoo قيد التشغيل)
node e2e-live.mjs
# اختبار الحِمل
k6 run guest_load.js
# اختبارات الأودو (قاعدة مؤقّتة معزولة)
docker exec odoo19 odoo -d <db> -i recycle_warehouse --test-enable \
  --test-tags=/recycle_warehouse --stop-after-init --max-cron-threads=0
```
