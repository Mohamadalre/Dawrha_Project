# تقرير الاختبارات — منصّة Dawrha

> كل الأرقام أدناه **مقيسة من تشغيل فعلي** على هذه البيئة (لا تقديرات). تاريخ القياس: 2026-08-17.
> البيئة: Backend (NestJS) على المضيف · PostgreSQL `dawrha_db`@127.0.0.1:5432 · Redis@localhost:6379 · Odoo 19.0 (حاوية) · k6 لاختبار الحِمل.

---

## 1) منهجية الاختبار

| الطبقة | الأداة | ما تقيسه |
|---|---|---|
| Unit (Backend) | Jest | خدمات/كنترولرات مع مستودعات (repositories) وهمية — منطق الأعمال معزولاً |
| E2E (Backend) | Jest + supertest عبر Nest `TestingModule` | الكنترولر + ValidationPipe + الحمايات + مغلّف الاستجابة (مستودعات وهمية، **ليست** قاعدة حيّة) |
| Coverage | Istanbul (clover) | نسبة تغطية الأسطر/الفروع/الدوال |
| Load/Perf | k6 | زمن الاستجابة والإنتاجية تحت حِمل على خوادم حيّة (Postgres + Redis) |
| Unit (Odoo) | إطار Odoo `TransactionCase` | منطق الأودو على قاعدة مؤقّتة (تُنشأ وتُحذف) |

**حجم معطيات الاختبار:** اختبارات الوحدة/E2E تستخدم تجهيزات صغيرة حتمية (١–٢٠ صفّاً لكل اختبار: محافظ، مستودعات، طلبيات…) مبنيّة داخل كل اختبار ثم تُلغى. اختبار الحِمل ضُرب على قاعدة **مُهاجَرة حديثاً (فارغة من البيانات)** — انظر تحذير الدقّة في القسم 4.

---

## 2) نتائج Backend — Unit + E2E + Coverage (مقيسة)

| المقياس | القيمة الحقيقية |
|---|---|
| سويتات الوحدة | **90** |
| اختبارات الوحدة | **938 / 938 ناجحة (100%)** |
| زمن تشغيل الوحدة | ~20 ثانية (79.6s مع أداة التغطية) |
| E2E سويتات/اختبارات | **8 / 70 ناجحة (100%)** — 6.96s |
| التغطية — الأسطر (Statements) | **49.90%** (6760/13548) |
| التغطية — الفروع (Branches) | **45.08%** (4591/10185) |
| التغطية — الدوال (Functions) | **39.35%** (968/2460) |
| عدد ملفات المصدر المقيسة | 407 |

> رُفعت التغطية بإضافة **4 ملفات اختبار جديدة (+18 اختباراً)** لملفات كانت 0%: `my-categories.service`, `my-categories.controller`, `suggestions`/`admin-suggestions` controllers, `favourites.controller`. (الأسطر: 48.82% → 49.90%.)

### اختبارات الوحدة حسب القسم (المجموع 938)

| القسم | اختبارات | ناجحة |
|---|---|---|
| waste-management (الكاتالوج/الهوم/العروض) | 335 | 335 |
| order (الطلبات) | 174 | 174 |
| account-management | 68 | 68 |
| odoo-sync | 60 | 60 |
| warehouse | 51 | 51 |
| onboarding (تسجيل الحسابات) | 49 | 49 |
| user | 32 | 32 |
| truck | 30 | 30 |
| **auth** | 29 | 29 |
| points-wallet (المحفظة/النقاط) | 27 | 27 |
| stages (المراحل) | 19 | 19 |
| notification | 16 | 16 |
| media | 14 | 14 |
| i18n | 11 | 11 |
| common / core / reports / platform-settings | 23 | 23 |

**ملفات E2E الثمانية:** account-status-scope · delivery-rate · driver-handover · google-account-signin · guest-app-routes · odoo-order-webhook · onboarding-response-shape · response-envelope.

---

## 3) نتائج Odoo — Unit (مقيسة، تشغيل معزول على قاعدة مؤقّتة)

| المقياس | القيمة الحقيقية |
|---|---|
| ملفات/أصناف الاختبار | **31** |
| دوال الاختبار (test methods) | **285** |
| الإصدار | Odoo Server 19.0 |
| أصناف نُفِّذت فعلاً | **25 من 31** |
| **إخفاقات (FAIL)** | **1** — `TestProductSuggestion.test_cannot_submit_twice` |
| **أخطاء (ERROR)** | **2** — `TestProductSuggestion.test_payload_carries_the_unit_code_not_its_label` · `test_backend_auto_sync.setUpClass` |
| بقيّة الأصناف الـ23 | مرّت دون FAIL/ERROR |

**تفصيل صادق:**
- التشغيل بـ`--test-tags=/recycle_warehouse` (لعزل اختبارات الوحدة عن اختبارات وحدات Odoo الأساسية).
- **توقّف عند الصنف الـ25 `test_report_arabic`** (توليد PDF عربي عبر wkhtmltopdf) — تعليق معروف في الحاويات بلا واجهة رسومية، **ليس خطأ برمجياً**؛ لذلك لم تُنفَّذ آخر 6 أصناف.
- **خطأ `test_backend_auto_sync` بيئي**: يتطلّب خادم NestJS الحيّ الذي لا تصله قاعدة الاختبار المعزولة (ليس عيباً في الكود).
- **إخفاقا `test_product_suggestion`** يستحقّان الفحص (قد يكونان حقيقيين) — أستطيع تشخيصهما عند الطلب.

> **الحكم:** غالبية اختبارات الوحدة للأودو **تمرّ**؛ المتبقّي إمّا بيئي أو تعليق أداة PDF، مع إخفاقين محدّدين في صنف واحد للمتابعة. أمر التشغيل:
> ```
> MSYS_NO_PATHCONV=1 docker exec odoo19 odoo -d <db> --db_host=db --db_user=odoo --db_password=odoo \
>   -i recycle_warehouse --test-enable --test-tags=/recycle_warehouse --stop-after-init \
>   --max-cron-threads=0 --http-port=8969 --gevent-port=8972
> ```

---

## 4) نتائج اختبار الحِمل/الأداء — k6 (مقيسة)

**الإعداد:** 30 VUs (ذروة) · 40 ثانية (تصاعد 10s→30 VU→هبوط) · 3 مسارات ضيف حقيقية تنفّذ استعلامات قاعدة بيانات:
`/api/v1/user-app/guest/{categories, products, offers}`.

| المقياس | القيمة الحقيقية |
|---|---|
| إجمالي الطلبات | **14,666** |
| الإنتاجية (throughput) | **366.6 طلب/ثانية** |
| طلبات نجحت (HTTP 200) | **300** |
| طلبات حُدّت (HTTP 429 — Rate limit) | **14,366** |
| زمن استجابة الطلبات الناجحة — متوسط | **20.4 ms** |
| — الوسيط p50 | **18.9 ms** |
| — p90 | **27.4 ms** |
| — p95 | **30.1 ms** |
| — أقصى | 138.9 ms |
| بيانات مستلمة | 6.0 MB |

### أهم استنتاج (نتيجة أمان حقيقية)
**محدِّد المعدّل (Rate Limiter) يعمل بفعالية:** تحت سيل من عنوان IP واحد، سمح الخادم بـ~**100 طلب/دقيقة** فقط (المجموع 300 طلب ناجح خلال النافذة المتحركة) و**رفض 14,366 طلباً بـ429** — حماية الخادم من الإغراق. زمن استجابة الطلبات الناجحة بقي منخفضاً (**p95 ≈ 30ms**).

### ب) على قاعدة **مليئة** (بيانات مُدرجة: 8 تصنيفات · 96 مادة · 384 صفّ تسعير)

نفس إعداد k6 (30 VUs · 40s)، بعد إدراج كتالوج حقيقي:

| المقياس | القيمة الحقيقية |
|---|---|
| إجمالي الطلبات | **31,377** |
| الإنتاجية | **784.4 طلب/ثانية** |
| طلبات نجحت (200) | 300 |
| طلبات حُدّت (429) | 31,077 |
| زمن الطلبات الناجحة (غالباً من الكاش) — p50 / p95 | **5.6 ms / 15.1 ms** |

**قياس منفصل — زمن مسار قاعدة البيانات (بعد تفريغ الكاش، 30 استعلاماً مميّزاً بلا كاش على 96 مادة):**

| المقياس | القيمة |
|---|---|
| p50 | **20.5 ms** |
| المتوسط | 26.2 ms |
| p90 / p95 | 40.1 ms / **41.2 ms** |
| min / max | 12.5 ms / 175 ms (أول طلب بارد) |

### جدول المقارنة (كلها مقيسة)
| السيناريو | p50 | p95 |
|---|---|---|
| قاعدة فارغة — طلبات ناجحة | 18.9 ms | 30.1 ms |
| قاعدة مليئة — إصابة كاش (Cache hit) | **5.6 ms** | **15.1 ms** |
| قاعدة مليئة — مسار DB حقيقي (Cache miss) | **20.5 ms** | **41.2 ms** |

**الخلاصة الواقعية:** على قاعدة مليئة، استعلام الكتالوج الحقيقي (مع الـjoins على التسعير) ≈ **20ms (p50) / 41ms (p95)**، والطلبات المتكرّرة تُخدَم من الكاش بـ**~5ms**. النظام حافظ على زمن استجابة منخفض تحت الحِمل.

### ج) السعة الخام — **بلا محدِّد معدّل** (رُفع الحدّ مؤقّتاً ثم أُعيد)
30 VUs · 40s · قاعدة مليئة · كل الطلبات تُخدَم فعلياً (لا 429):

| المقياس | القيمة الحقيقية |
|---|---|
| إجمالي الطلبات الناجحة (200) | **9,398** — **0 فشل (0.00%)** |
| **الإنتاجية الفعلية (طلبات مُخدَّمة/ث)** | **234.9 طلب/ثانية** |
| زمن الاستجابة — المتوسط | 63.1 ms |
| — p50 | **57.8 ms** |
| — p90 | 107.8 ms |
| — p95 | **117.9 ms** |
| — أقصى | 758 ms |
| بيانات مستلمة | 32 MB |

**التفسير:** حين يُرفع المحدِّد، تُخدَم **كل** الطلبات (9,398 بلا فشل). النسخة الواحدة تحمّلت **~235 طلباً مُخدَّماً/ثانية** عند 30 مستخدماً متزامناً، وزمن الاستجابة ارتفع (p50 58ms) بسبب تنافس 30 اتصالاً على عملية Node واحدة + بركة اتصالات القاعدة — سلوك طبيعي عند التشبّع. (تمّت إعادة الحدّ إلى 100/60s بعد القياس.)


