# تقرير المتطلبات غير الوظيفية (Non-Functional Requirements) — منصّة Dawrha

> **منهجية التوثيق:** كل بند أدناه مُستخرَج بالقراءة المباشرة للشيفرة المصدرية، ومقرونٌ بمرجعه (ملف: سطر) ليكون قابلاً للتحقّق. حيث لا يوجد تطبيق، ذُكر ذلك صراحةً بلا تجميل. الأرقام القياسية مأخوذة من تشغيل فعلي على البيئة (انظر `VERIFICATION_REPORT.md`).
> **المعمارية:** NestJS 11 + TypeScript 5.7 · PostgreSQL 16 / TypeORM (هجرات فقط، `synchronize:false`) · Redis 7 (كاش + طوابير + محدّد معدّل) · BullMQ · Socket.IO · Firebase · Cloudinary · تكامل ثنائي الاتجاه مع Odoo 19.
> آخر تحديث: 2026-08-18.

---

## 0) خلاصة تنفيذية

| المجال | التقييم | الشاهد المختصر |
|---|---|---|
| الأمن | ✅ قوي متعدّد الطبقات | Argon2 · JWT ثلاثي · **رمز تحديث مُجزَّأ + تدوير** · Throttler على Redis · Webhooks موقّعة بمقارنة زمن-ثابت · Joi fail-fast · Helmet+CORS |
| الأداء | ✅ مطبَّق ومقيس | كاش Redis بإبطال O(1) · 64 فهرساً · ترقيم مُتحقَّق · طوابير للعمل الثقيل · **وسائط على Cloudinary CDN** — كتالوج مُخدَّم ≈ p95 15–56ms |
| قابلية التوسّع | ✅ | مصادقة بلا حالة · Redis مشترك · عملية Worker مستقلّة |
| الموثوقية | ✅ | 3 مرشّحات استثناء · معاملات ذرّية · طوابير بإعادة محاولة · **مصالحة Odoo كشبكة أمان (12 كرون)** |
| قابلية الصيانة | ✅ | معمارية معيارية · 944 اختبار وحدة + 8 سويتات E2E · 91 هجرة مُصدَّرة |
| التدويل | ✅ متقدّم | ثنائية اللغة مع فرض تغطية آلي · **لغة لكل حساب بلا هيدر** |
| المراقبة | ✅ | Winston دوّار + Morgan + سجلّ تدقيق |
| الأمن — Helmet/CORS | ✅ مطبَّق ومُثبَت حيّاً | HSTS · nosniff · X-Frame-Options · Referrer-Policy · Cross-Origin-* · CORS بقائمة بيضاء (تفصيل في 1.7) |

---

## 0.1) التقنيات المستخدمة (Technology Stack)

| الطبقة | التقنية | الدور في المشروع |
|---|---|---|
| اللغة/الإطار | **TypeScript 5.7 + NestJS 11** | إطار معياري بحقن تبعيات، أنواع صارمة |
| قاعدة البيانات | **PostgreSQL 16 + PostGIS** | تخزين علائقي + أنواع `geography` للمواقع والمسافات |
| ORM/الهجرات | **TypeORM 0.3** | كيانات + **هجرات فقط** (`synchronize:false`) + استعلامات QueryBuilder |
| الكاش/الطوابير/الحدّ | **Redis 7 + ioredis** | كاش الكتالوج، حالة الطوابير، محدّد المعدّل الموزّع، كاش الصلاحيات والجلسات |
| الطوابير الخلفية | **BullMQ** | 6 طوابير (بريد/OTP، مزامنة Odoo، إشعارات) بإعادة محاولة + backoff |
| اللحظي (Realtime) | **Socket.IO** | تتبّع الشاحنات لحظياً (namespace `/tracking`) + غرف الطلبات |
| المصادقة | **Passport + JWT + Argon2 + Google OAuth** | JWT ثلاثي، تجزئة Argon2، دخول اجتماعي |
| الأمن | **Helmet + CORS + @nestjs/throttler** | ترويسات أمان، قائمة أصول بيضاء، تحديد معدّل |
| التحقّق | **class-validator + class-transformer + Joi** | تحقّق DTO + تحويل الأنواع + تحقّق إعدادات البيئة |
| التدويل | **nestjs-i18n** | ثنائية اللغة (ar/en) + لغة لكل حساب |
| السجلّات | **Winston + winston-daily-rotate-file + Morgan** | سجلّ منظّم دوّار + سجلّ HTTP |
| الوسائط/الإشعارات/البريد | **Cloudinary · Firebase Admin (FCM) · Nodemailer** | صور CDN، إشعارات دفع، بريد/OTP |
| الجدولة | **@nestjs/schedule** | كرونات الصيانة والمصالحة (في عملية الـWorker) |
| التكامل الخارجي | **Odoo 19 (addon `recycle_warehouse`) — JSON-RPC + Webhooks** | نظام المستودعات/الأسطول/الفوترة |
| الاختبار/الجودة | **Jest + Supertest · ESLint + Prettier · k6** | وحدة/تكامل، تنسيق، اختبار حِمل |

---

## 1) الأمن (Security)

### 1.1 المصادقة (Authentication)
| العنصر | ما طُبِّق | الدليل في الكود |
|---|---|---|
| تجزئة كلمة المرور | **Argon2** (لا bcrypt) — من أقوى الخوارزميات الحديثة (فائز مسابقة PHC) | `auth.service.ts`, `account-management.service.ts`, `auth/exceptions/auth.exceptions.ts` |
| رموز JWT | **ثلاثة أنواع بأسرار منفصلة** | `auth.service.ts:469` (`JWT_ACCESS_SECRET`), `:473` (`JWT_REFRESH_SECRET`), `:527/:554` (`JWT_TEMPORARY_SECRET`) |
| ربط الجهاز بالتوكن | حِمل التوكن يحمل **`deviceId`** لتتبّع أي جهاز أصدره + خروج لكل جهاز | `auth.service.ts:460` (payload)، `jwt.strategy.ts` (يُعيد `deviceId`) |
| «تذكّرني» | مدّة refresh أطول عند الطلب | `JWT_REFRESH_REMEMBER_ME_EXPIRATION` مقابل الافتراضي |
| الدخول الاجتماعي | **Google OAuth** لكل تطبيق (user/collector/factory) | `auth.controller.ts` (`login/*/google`) |
| تحقّق البريد / نسيت كلمة المرور | **OTP** مُجزّأ SHA-256، TTL، تهدئة (cooldown)، حدّ يومي، قفل محاولات، تحقّق زمن-ثابت؛ ونسيت كلمة المرور بلا كشف تعداد الحسابات + إعادة إرسال محميّة | `auth.service.ts` (`resendOtpCode`, `resendForgotPasswordOtp`) — رواتات الإعادة عليها `@Throttle` + cooldown + `OTP_MAX_DAILY_RESENDS` |

### 1.2 التفويض (Authorization) — دفاع متعدّد الطبقات
- **بوابات متتالية:** `JwtAuthGuard` (هوية) → `AccountStatusGuard` (حالة الحساب — فعّال/محظور/بانتظار) → `RolesGuard` / `PermissionsGuard` (الدور والصلاحية الدقيقة).
- **كتالوج صلاحيات ديناميكي** (`permissions` + `role_permissions`) يُفرَض بـ`@Permissions('key')` وكاش Redis؛ إضافة صلاحية = سطر بالكتالوج + سطر بالخريطة.
- **عزل حسب الشريحة على مستوى البيانات:** لا يستطيع توكن مستخدم قراءة أسعار المعامل — السعر يُشتق من دور التوكن حصراً، والحالات (grades) لا تظهر إطلاقاً لغير المعامل/الجهة الحرة (`catalog.service.ts::mapProduct` / `mapOffer`).

### 1.3 صلابة المدخلات (Input Hardening)
- **`ValidationPipe` عام** (`main.ts:49–52`): `whitelist:true` + `forbidNonWhitelisted:true` + `transform:true` → يرفض أي حقل غير معرّف (يمنع Mass-Assignment) ويحوّل الأنواع.
- **الترقيم مُتحقَّق كأرقام موجبة** عبر المشروع: `page/limit` بـ`@Type(()=>Number)` + `@IsInt()` + `@Min(1)` + `@Max` (`waste-management/common/dto/pagination.dto.ts`)؛ تُدقّق حتى الرواتات ذات المعامل المفرد (`truck-tracking.controller.ts`, `HistoryQueryDto`).
- **`ClassSerializerInterceptor`** عام (`main.ts:73`) → يُخفي الحقول الحسّاسة (تجزئة كلمة المرور…) من الردود.

### 1.4 تحديد المعدّل (Rate Limiting)
- **Throttler موزّع على Redis** (`app.module.ts:74`): `100 طلب / 60 ثانية / (IP+مسار)`، تخزين `ThrottlerStorageRedisService` مشترك بين كل النسخ.
- **تشديد على المسارات الحسّاسة** عبر `@Throttle()`: التسجيل 10/دقيقة (`auth.controller.ts:29`)، الدخول وإعادة إرسال الرموز 5/دقيقة (`auth.controller.ts:133`).
- **مثبت بالقياس الحقيقي (k6):** تحت سيل من IP واحد رفض الخادم عشرات الآلاف من الطلبات بـ429 وأبقى الطلبات المشروعة بزمن منخفض (تفاصيل الأرقام في `VERIFICATION_REPORT.md`).

### 1.5 أمن رواتات التكامل مع Odoo (الاتجاهان)
التواصل بين النظامين على قناتين، لكلٍّ مصادقتها — **تحقّقتُ من حماية كل رواتة**:

**أ) Odoo → الباك يند (Webhooks الواردة):** **10 رواتات** مواجهة للأودو، كلها محميّة بـ**مقارنة زمن-ثابت** `timingSafeEqual` ضدّ `ODOO_WEBHOOK_SECRET` عبر حارس واحد `assertAuthorized` (`odoo-webhook.controller.ts:274`):
`inventory · warehouse · fleet · products · orders · delivery-tariffs · driver-decision · shift-change-decision · product-suggestion` (+ `delivery` في `order/delivery-webhook.controller.ts`). لا رواتة واحدة بلا تحقّق (الرواتات المُجمّعة تمرّ عبر `queueSync` الذي يستدعي الحارس أيضاً).
- **Fail-safe:** سرّ غير مضبوط → المنافذ **معطَّلة** بالكامل، فلا يُقبل webhook غير مُوثَّق أبداً.
- **تشديد معدّل** على المسار الأكثر عرضة (`product-suggestion`): `@Throttle 20/60s`.
- **بلا JWT عمداً:** هذه رواتات آلة-إلى-آلة، فالمصادقة بالسرّ المشترك لا بتوكن مستخدم.

**ب) الباك يند → Odoo (JSON-RPC الصادرة):** نداءات `callKw` عبر جلسة Odoo مُصادَقة (`ODOO_USERNAME`/`ODOO_PASSWORD`)، مع **تخزين معرّف الجلسة في Redis (~25 دقيقة)** و**مهلة 10s** لكل نداء (`odoo/odoo.service.ts`) — فلا إعادة مصادقة على كل طلب ولا تعليق لا نهائي عند تعذّر الوصول.

### 1.6 إدارة الأسرار (Secrets)
- كل الأسرار في `.env` **خارج الكود** (DB, JWT×3, Redis, Cloudinary, Firebase, Google, Odoo, SMTP) مع **تحقّق Joi** لمخطّط الإعدادات (`core/config/configuration.ts:3` — `Joi.object({...})`) → **فشل مبكر (fail-fast)** عند سوء الإعداد بدل انهيار وقت التشغيل.
- الأسرار غير مرفوعة على git (`.gitignore`).

### 1.7 ترويسات الأمان والأصول (Helmet + CORS) — **مطبَّق**
- **Helmet** (`main.ts`, قبل أي middleware): يُرسِل حزمة ترويسات أمان على **كل** استجابة (نجاحاً وخطأً) — مُثبَت بالقياس الحيّ:
  `Strict-Transport-Security: max-age=31536000; includeSubDomains` (HSTS) · `X-Content-Type-Options: nosniff` (منع MIME-sniffing) · `X-Frame-Options: SAMEORIGIN` (منع clickjacking) · `Referrer-Policy: no-referrer` · `Cross-Origin-Opener-Policy` / `Cross-Origin-Resource-Policy` · `Origin-Agent-Cluster` · و**إزالة بصمة `X-Powered-By`**.
  - `contentSecurityPolicy` مُعطَّل عمداً لأن العملية تخدم API بصيغة JSON (لا SPA)، ويُفعَّل بسياسة صريحة يوم تُخدَم واجهة متصفّح من هنا.
- **CORS** (`main.ts`): **قائمة أصول بيضاء** لا wildcard — الأصول من `CORS_ORIGINS` (مفصولة بفواصل، مُتحقَّقة في Joi) لكل بيئة؛ عند عدم ضبطها (تطوير محلّي) يُعكَس أي أصل. مسموح بالـ`credentials` والترويسات `Authorization, x-lang, lang, x-odoo-webhook-secret`. مُثبَت حيّاً (`Access-Control-Allow-Origin` يعكس الأصل المُرسَل).

### 1.8 حماية رمز التحديث (Refresh Token) — قصّة كاملة
رمز التحديث هو الأطول عمراً وأخطرها لو تسرّب، فعومل بعناية خاصّة (`auth.service.ts`):
- **لا يُخزَّن أبداً كنصّ صريح.** يُولَّد الرمز الخام ثم **يُجزَّأ بـArgon2** (`argon2.hash(refreshTokenRaw)`, `:481`) قبل أي تخزين — فالخام يصل للعميل فقط، وتسرّب القاعدة أو Redis لا يكشف رمزاً صالحاً.
- **تخزين مزدوج للصمود:** الهاش يُحفَظ في **صفّ الجهاز** (`device.refreshToken`) **وفي Redis** بمفتاح `refreshToken:<deviceId>` (`:502, :507`) — Redis للسرعة، والقاعدة احتياط عند فقد الكاش.
- **مربوط بالجهاز:** المفتاح لكل `deviceId`، فكل جهاز له رمزه المستقلّ، والخروج يمسح مفتاح Redis وصفّ الجهاز معاً (`:437–438`).
- **تحقّق زمن-آمن + تدوير (Rotation):** عند التحديث يُقرأ الهاش (Redis أولاً ثم القاعدة) ويُتحقّق بـ`argon2.verify` (`:591`)؛ وعند النجاح **يُصدَر زوج رموز جديد كليّاً** (`generateTokens`, `:595`) — فالرمز المُستخدَم يُستبدَل، ورمز خاطئ يُرفَض بـ`Access denied`.
- **قطع فوري:** القائمة السوداء في Redis (`blackListToken:<userId>`) تُبطِل توكناً حتى قبل انتهائه (تُفحَص في المصادقة وفي مصافحة السوكيت).

### 1.9 رمز التحقّق (OTP) — قصّة كاملة
يُستخدَم لتأكيد البريد ولنسيت كلمة المرور، بحماية متعدّدة الطبقات (`core/mail/mail.service.ts`):
- **مُجزَّأ لا صريح:** الرمز يُخزَّن في Redis **بتجزئة SHA-256** (`createHash('sha256')`) لا كنصّ — فتسرّب الكاش لا يكشف الرمز.
- **عمر قصير:** `OTP_TTL_SECONDS = 600` (10 دقائق) ثم يُبطَل تلقائياً (Redis `EX`).
- **تهدئة بين الإرسالات:** `OTP_COOLDOWN_SECONDS = 120` (مفتاح cooldown) — يمنع القصف بالبريد.
- **قفل التخمين:** `OTP_MAX_ATTEMPTS = 5` محاولات خاطئة → قفل؛ وعدّاد المحاولات يُصفَّر عند النجاح.
- **سقف يومي:** `OTP_MAX_DAILY_RESENDS = 10` إعادة إرسال لكل بريد/يوم (ضدّ email-bombing).
- **إرسال عبر طابور (لا يحجب الطلب):** يُوضَع في طابور البريد BullMQ (retry + backoff)؛ وإن فشل وضعه في الطابور تُلغى التهدئة (rollback) فلا يعلَق المستخدم.
- **لا كشف تعداد:** نسيت كلمة المرور تُرجع رسالة موحّدة سواء وُجد البريد أم لا، مع مقارنة زمن-آمنة.

---

## 2) الأداء (Performance)
| الأسلوب | الدليل في الكود |
|---|---|
| **كاش Redis للكتالوج** بإبطال O(1) بعدّاد نسخة (بلا `SCAN/KEYS`) | `waste-management/common/providers/catalog-cache.service.ts` — TTL: تصنيفات/منتجات **12h**، عروض **6h** (`:25–27`) |
| **عدم تخزين النتائج الفارغة** (منع «الصفحة الافتراضية الفارغة تُثبَّت 12 ساعة») | `guest-app.service.ts` + `catalog.service.ts` — `if (total > 0) cache.set(...)` |
| **ترقيم صفحات** في كل القوائم | `PaginationQueryDto` عبر الكتالوج/الطلبات/الحسابات… |
| **فهرسة قاعدة البيانات** | **64 `@Index`** + **~42 قيد فرادة (unique)** موزّعة على الكيانات |
| **تصفية على مستوى SQL** (لا في الذاكرة) | استعلامات `EXISTS` مترابطة في `catalog.service.ts` (التسعير/المخزون/العروض) لإبقاء الترقيم والعدّ صادقَين على نفس المجموعة |
| **إزاحة العمل الثقيل خارج مسار الطلب** | 6 طوابير BullMQ (بريد/OTP، مزامنة Odoo، إشعارات) |
| **حساب المسافات مرّة واحدة** | كاش `distance_cache` لأزواج (مشترٍ×مستودع) + احتياطي خط مستقيم مؤقّت يُرقّى لاحقاً (`order/providers/distance.service.ts`) |

**أرقام مقيسة (بيئة حيّة، قاعدة مليئة):** استعلام الكتالوج مع الـjoins ≈ p50 20ms / p95 41ms على المسار البارد، وإصابة الكاش ≈ 5–15ms؛ السعة الخام (بلا محدّد) ≈ 670 طلب/ثانية بلا فشل عند 30 مستخدماً متزامناً. التفاصيل في `VERIFICATION_REPORT.md`.

### 2.1 تخزين الوسائط على السحابة (Cloudinary CDN)
الصور (صور المنتجات/التصنيفات، مستندات الحسابات، الشعارات) **لا تُخزَّن على خادم التطبيق ولا في القاعدة** — تُرفَع إلى **Cloudinary** ويُحتفَظ في القاعدة **بالرابط (URL) فقط** (`core/cloudinary/cloudinary.service.ts`, `cloudinary.module.ts`):
- **رفع تدفّقي بلا قرص مؤقّت:** الملف يُمرَّر عبر `uploader.upload_stream` مباشرة من الذاكرة (`multer` memory storage) إلى Cloudinary — فلا ملفات مؤقّتة تتراكم على الخادم.
- **لماذا سحابة؟** (١) **الأداء:** تُخدَم الصور من شبكة توصيل محتوى (CDN) عالمية لا من خادم التطبيق، فلا تُثقِل مساره. (٢) **قابلية التوسّع:** التخزين بلا حالة على الخادم، فأي نسخة API تعمل بلا تخزين محلّي مشترك. (٣) **المرونة:** تحويلات الصور (حجم/قصّ) عند الطلب عبر الرابط.
- **دورة الحياة:** إنشاء عبر `upload`، حذف عبر `destroy(publicId)` (فلا صور يتيمة عند حذف الكيان)، ومعاملات الوسائط ملفوفة بـtransactions مع rollback (`media.service`).
- **الأسرار** (`CLOUDINARY_CLOUD_NAME/API_KEY/API_SECRET`) في `.env` ومُتحقَّقة بـJoi.

---

## 3) قابلية التوسّع (Scalability)
- **مصادقة بلا حالة (Stateless JWT)** → توسّع أفقي بلا التصاق جلسة.
- **Redis مشترك** للـThrottle والكاش وحالة الطوابير → سلوك متّسق عبر عدّة نسخ للـAPI.
- **عملية Worker منفصلة** (`main.worker.ts` / `npm run start:worker`) → توسيع العمل الخلفي (كرونات الصيانة والمصالحة) مستقلّاً عن مسار HTTP.
- **قياس:** نسخة واحدة خدمت مئات الطلبات/الثانية (§2).

---

## 4) الموثوقية والصمود (Reliability & Resilience)
| العنصر | الدليل في الكود |
|---|---|
| **مرشّحات استثناء عامة (3 طبقات)** | `main.ts:61–63` — `LoggerExceptionsFilter` / `AllExceptionsFilter` / `DatabaseExceptionFilter` |
| **معالجات على مستوى العملية** | `main.ts:92` (`uncaughtException`) و`:101` (`unhandledRejection`) — تمنع سقوط العملية الصامت |
| **العمليات الذرّية (Transactions)** | **7 ملفات** تستخدم `dataSource.transaction` (المراحل، سعر التوصيل، الوسائط مع rollback، إنشاء الطلب…) |
| **الحتمية (Idempotency)** | دفع أجزاء الطلب يقصُر عند وجود `odooOrderId`؛ webhook الاقتراح يُحدّث بنفس `odoo_suggestion_id` بدل التكرار؛ إنشاء المحفظة idempotent |
| **إعادة المحاولة مع backoff + تعويض** | طوابير BullMQ (retry) + تعويض «احذف-إن-لم-يُزامَن-قط» لمزامنة Odoo |
| **مصالحة Odoo — شبكة الأمان الأهم** | **12 كرون مصالحة** في `odoo-sync/*reconcile*` تُعيد سحب الحالة كاملة كل ~10 دقائق (أسطول، مستودعات، دفع الكتالوج، حالة السائق، الطلبات…)، فأي ping يُفقَد بانقطاع اتصال يُصلَّح تلقائياً. **يعمل ضمن عملية الـWorker** |
| **مرئية الأعطال (تحسين هذه الجولة)** | فشل المزامنة الصادرة من Odoo لغياب الإعداد رُفِع من صامت (DEBUG) إلى **WARNING** برسالة عمليّة، مع واجهة إعدادات مخصّصة لضبط رابط الباك يند والسرّ (`recycle_warehouse: res_config_settings + backend_sync.py`) |
| **هجرات مُصدَّرة** | **91 هجرة** مُرقّمة، آمنة ضمن transaction؛ `synchronize:false` (لا تعديل مخطّط تلقائي في الإنتاج) |

---

## 4.1) المعالجة غير المتزامنة — طوابير BullMQ (قصّة كاملة)
كل عمل قد يبطئ أو يفشل خارج سيطرتنا (نداء Odoo، إرسال بريد، دفع إشعار) **يُزاح إلى طابور BullMQ على Redis** بدل تنفيذه داخل مسار الطلب (`odoo-sync.service.ts`, `core/queue`, `core/mail`, `notification`):
- **ثلاثة طوابير:** `waste-odoo-sync` (مزامنة الأودو)، `mail-queue` (بريد/OTP)، `notification-queue` (إشعارات).
- **سياسة إعادة محاولة لكل نوع مهمة** (`odoo-sync.constants.ts`): `attempts` (2–3) و`backoff` **من نوع أُسّي (exponential)** بتأخير أوّلي 2s–10s حسب حساسية المهمة — فمزامنة المخزون تُعيد المحاولة بلطف أكبر من دفع سعر.
- **`removeOnComplete: true`** (لا تتراكم المهام الناجحة) و**`removeOnFail: false`** (تُحفَظ الفاشلة للفحص) (`odoo-sync.service.ts:53–54`).
- **تعويض عند الفشل النهائي:** بعد استنفاد المحاولات، منطق تعويضي (مثل «احذف-إن-لم-يُزامَن-قط») + إشعار الأدمن — فلا يبقى النظامان متضاربين بصمت.
- **الفائدة:** مسار الطلب يبقى سريعاً (يردّ فوراً بعد وضع المهمة)، والعمل الثقيل يجري في الخلفية بصمود ضدّ الانقطاعات المؤقتة.

## 4.2) الإشعارات الفورية — Firebase FCM (قصّة كاملة)
الإشعارات الفورية (قرارات الحساب، أحداث الطلب…) تُرسَل عبر **Firebase Cloud Messaging** (`notification/services/firebase.service.ts`):
- **بثّ متعدّد (Multicast):** `messaging().sendEachForMulticast` يرسل لكل أجهزة المستخدم في نداء واحد.
- **إشعار مُترجَم لكل جهاز:** الأجهزة تُجمَّع **حسب لغتها** (`notification.processor.ts`)، فيصل لكل جهاز عنوان/نصّ بلغته المحفوظة (نفس فلسفة i18n).
- **تنظيف الرموز الميتة تلقائياً:** الاستجابة تُفحَص لكل رمز؛ رمز `invalid-registration-token`/`UNREGISTERED` يُجمَع ثم **يُحذَف من القاعدة** (`invalidateDeviceTokens`) — فلا نُرسِل لأجهزة ميتة للأبد.
- **عبر طابور بإعادة محاولة** (`notification-queue`, `NOTIFICATION_MAX_ATTEMPTS = 3`) + **حفظ داخل التطبيق (in-app)** مع soft-delete، فالإشعار لا يُفقَد لو كان الجهاز غير متصل.
- **نمط «أفضل جهد»:** فشل الدفع لا يُفشِل العملية الأساسية (استلام الطلب يكتمل حتى لو تعذّر الإشعار).
- **أسرار Firebase** (`FIREBASE_PROJECT_ID/CLIENT_EMAIL/PRIVATE_KEY`) مطلوبة ومُتحقَّقة بـJoi.

---

## 5) قابلية الصيانة (Maintainability)
- **معمارية معيارية (Modular)** بوحدات NestJS مستقلّة لكل سياق (auth, catalog, cart, order, warehouse, truck, odoo-sync…).
- **TypeScript** بأنواع صارمة (`tsc --noEmit` نظيف).
- **تغطية اختبار مقيسة:** **944 اختبار وحدة (90 سويت)** + **8 سويتات E2E** — كلها ناجحة؛ تغطية أسطر ≈ 50% (`coverage/clover.xml`).
- **مغلّف استجابة موحّد** (`TransformInterceptor`) → عقد API ثابت `{success, message, data, statusCode, timestamp}` بلا تعشيش.
- **استثناءات مسماة لكل موديول** بـ`errorCode` قابل للقراءة آلياً.
- **تحقّق إعدادات Joi** عند الإقلاع.

---

## 6) التدويل (Internationalization / i18n)
- **`nestjs-i18n`** بلغتين (en/ar)، fallback إنجليزي (`app.module.ts:57`)، بمحدِّدات: `x-lang`/`lang` (Header) + Query + Accept-Language (`:63–65`).
- **لغة لكل حساب — بلا هيدر (متقدّم):** المستخدم المسجَّل يضبط لغته مرّة في الإعدادات، فتعود **كل** الردود بها دون إرسال هيدر لكل طلب. اللغة تُقرأ طازجة من الحساب وتفوز على الهيدر (`common/interceptors/transform.interceptor.ts:33` + `all-exceptions.filter.ts`)، فتغيير اللغة يسري من الطلب التالي بلا إعادة دخول.
- **فرض تغطية الترجمة آلياً:** اختبار يُلزم كل رسالة/استثناء بمدخل عربي وإنجليزي (`i18n/translations.spec.ts`) → يمنع تسرّب نصوص إنجليزية للواجهة العربية.

---

## 7) المراقبة والتتبّع (Observability) — قصّة السجلّات
نظام تسجيل مبنيّ على **Winston** بفلسفة «سجلّ منظّم قابل للبحث» لا مجرّد طباعة (`core/logger-config/winston.config.ts`):

- **صيغتان حسب البيئة:** في الإنتاج **JSON منظّم** (`timestamp + level + context + channel + stack + metadata`) جاهز لأنظمة التجميع (ELK/Datadog…)؛ في التطوير **إخراج ملوّن مقروء** للكونسول.
- **قنوات (Channels) لفصل التدفّقات:** لكل سطر `channel` (مثل `app`, `jobs`, `orders`, `http`)، ودوالّ `filterByChannel` / `filterExcludeChannels` تُوجِّه كل قناة إلى ملفّها — فسجلّ الطوابير لا يختلط بسجلّ الطلبات.
- **تدوير يومي مع أرشفة** (`DailyRotateFile`): ثلاثة مسارات — **`exceptions/` (مستوى error فقط)**، **`combined/`**، **`app/`** — كلها `datePattern: YYYY-MM-DD` · **`maxFiles: 10d`** (احتفاظ 10 أيام) · **`maxSize: 20m`** · **`zippedArchive: true`** (ضغط الأقدم) — فلا تمتلئ القرص ولا تُفقَد الأدلّة.
- **اعتراض HTTP كامل:** `LoggerHttpInterceptor` + **Morgan** يسجّلان كل طلب/استجابة (المسار، الحالة، الزمن).
- **معالجة الأخطاء غير الملتقطة** تُسجَّل في قناة الاستثناءات قبل أي سقوط (`main.ts` process handlers).
- **سجلّ تدقيق (Audit Trail):** `AuditService.record({action, entityType, entityId, oldValues, newValues, userId})` لكل تعديل إداري حسّاس — «مَن غيّر ماذا ومتى».
- **إسكات الكونسول في الإنتاج** (`NODE_ENV==='production'`) لتفادي تسرّب تفاصيل للمخرجات القياسية.

---

## 7.1) الاتصال اللحظي (Realtime — Socket.IO)

تتبّع الشاحنات لحظياً مبنيّ على **Socket.IO** بنمط الغرف (rooms)، على namespace مخصّص `/tracking` (`truck/tracking/truck-tracking.gateway.ts`):

- **مصادقة عند المصافحة (handshake):** كل اتصال يُتحقّق من توكن JWT في `handleConnection` عبر `JwtService`، **مع فحص القائمة السوداء في Redis** (`blackListToken:<userId>`) — فتوكن مُبطَل لا يفتح قناة لحظية، وأي اتصال بلا توكن صالح يُرفض فوراً (`error: Unauthorized`).
- **تدفّق البيانات:** السائق (COLLECTOR) يبثّ `truck:location {truckId, lat, lng}` → يُخزَّن ثم يُبَثّ إلى **غرفة الشاحنة** `truck:<id>` وإلى غرفة **`admins`**. الأدمن يشترك بـ`truck:subscribe` بشاحنة محدّدة أو `admin:subscribeAll` لكل الأسطول.
- **دورة حياة التوقّف:** `truck:stop` → يُثبَّت في القاعدة ويُبَثّ `truck:stopped`؛ و**انقطاع اتصال السائق يُنهي الرحلة تلقائياً** (`finalizeStop` بسبب `DRIVER_DISCONNECT`) فلا تبقى رحلة «معلّقة» بلا نهاية.
- **قابلية التوسّع:** التفويض على مستوى الغرفة (مالك/أدمن فقط)، والحالة في Redis — فيمكن تشغيل عدّة نسخ خلف موازن حِمل.

---

## 8) تصميم الـAPI والتوافق
- **بادئة عامة `api`** (`main.ts:38`) + **ترقيم إصدارات URI** (`main.ts:41`, `VersioningType.URI`) → `/api/v1/...` مع تطوّر متوافق مع الخلف؛ رواتات محايدة النسخة (الإشعارات) تعمل على المسارين معاً.
- **عقد استجابة موحّد** عبر كل المسارات (نجاحاً وخطأً).

---

## 9) المتطلبات غير الوظيفية في Odoo (addon `recycle_warehouse`)
- **معمارية وحدة (Addon) مستقلّة** لا تلمس نواة Odoo.
- **تكامل آمن ثنائي الاتجاه:** صادر من الباك يند عبر طوابير (كتابة)، ووارد إلى الباك يند عبر webhooks موقّعة (قراءة/مرآة)، مع **تبنّي تلقائي** لما يُنشأ في Odoo (مستودع/شاحنة جمع).
- **سلامة المخزون:** حجز all-or-nothing (`reserve_for_order`) ثم خصم فعلي مع تحرير الحجز — بلا خصم مزدوج (`models/stock.py`).
- **مطابقة الحالة إلزامية** للوحدات غير الوزنية عند الفرز، وتحويل `unit_code → uom_id` في `create` و`write` (تعديل هذه الجولة).
- **تقارير PDF (QWeb) بدعم العربية** (خطوط مُضمَّنة عبر fontconfig).
- **تغطية اختبار:** إطار `TransactionCase` (مئات دوال الاختبار)؛ نتائج التشغيل الفعلي في `VERIFICATION_REPORT.md`.

---

## 9.1) شرح تفصيلي لأربع آليات محورية

### أ) عملية الـWorker المنفصلة (Worker Process)
النظام يعمل بـ**عمليتين من نفس الشيفرة**:
- **`main.ts` (الـAPI):** يخدم طلبات HTTP وSocket.IO فقط — يبقى سريع الاستجابة.
- **`main.worker.ts` (الـWorker):** يشغّل **كرونات الصيانة والمصالحة فقط** (يُفعَّل بـ`MAINTENANCE_WORKER=true`، أمر `npm run start:worker`).

**لماذا الفصل؟** العمل الدوري الثقيل (تنظيف، إعادة محاولة، **مصالحة Odoo** كل ~10 دقائق) لو عمل داخل عملية الـAPI لتنافس مع طلبات المستخدمين على المعالج وبركة اتصالات القاعدة. الفصل يسمح بـ**توسيع كل طرف مستقلّاً** (عدّة نسخ API خلف موازن حِمل + Worker واحد)، ويضمن أن **شبكة أمان المزامنة تعمل دائماً** بمعزل عن حِمل HTTP. **تنبيه تشغيلي مهم:** كرونات المصالحة (التي تُصلّح أي ping مزامنة مفقود) لا تعمل إلا إذا كانت عملية الـWorker قيد التشغيل — فيجب إبقاؤها حيّة في الإنتاج.

### ب) تحقّق الإعدادات بـJoi (fail-fast Configuration)
كل متغيّرات البيئة تمرّ عبر **مخطّط Joi** (`core/config/configuration.ts` — `validationSchema = Joi.object({...})`) عند إقلاع التطبيق:
- الأسرار الإلزامية (`DB_*`, `REDIS_HOST`, أسرار JWT الثلاثة، `GOOGLE_*`, `FIREBASE_*`, `ODOO_*`…) معلّمة `.required()` — **غيابها يمنع الإقلاع فوراً برسالة واضحة** بدل انهيار غامض وقت أول استخدام.
- الاختيارية (`REDIS_PASSWORD`, `CORS_ORIGINS`, `PORT`…) معلّمة `.optional()` بقيَم افتراضية.
**الفائدة:** خطأ إعداد يُكتشَف لحظة الإقلاع (fail-fast) لا في الإنتاج بعد ساعات — وهو ما يميّز نظاماً جاهزاً للتشغيل.

### ج) الكاش على مستوى الرواتات (Per-Route Caching)
قوائم الكتالوج (تصنيفات/منتجات/عروض) تُخدَم عبر **كاش قراءة-عبور (read-through)** في Redis (`catalog-cache.service.ts`):
- **مفتاح لكل رواتة ومعاملاتها:** `catalog:<مورد>:v<N>:<الجمهور>:<الصفحة>:<الحدّ>:<البحث>` — فكل تركيبة (دور، صفحة، حدّ، بحث) لها إدخالها.
- **إبطال O(1) بعدّاد نسخة:** أي تعديل إداري (إنشاء/تعديل/حذف تصنيف أو منتج أو عرض أو سعر) يزيد عدّاد النسخة `INCR`، فتُهمَل **كل** المفاتيح القديمة فوراً بلا `SCAN/KEYS` (مكلفة). TTL: تصنيفات/منتجات 12h، عروض 6h.
- **حارس عدم تخزين الفارغ (تحسين هذه الجولة):** لا تُخزَّن نتيجة فارغة إطلاقاً — كتالوج فارغ حالة إقلاع عابرة، وتخزينها 12 ساعة كان يُسبّب «الصفحة الافتراضية لا تُظهر شيئاً حتى تنتهي المدة».
- **استثناء الطزاجة:** قوائم المعامل/الجهة الحرة (المقيّدة بالمخزون الحيّ في محافظتها) **لا تُكاش** لأن توافرها يتغيّر لحظياً.

### د) الترقيم (Pagination)
كل القوائم في المشروع تُرقَّم عبر **`PaginationQueryDto` موحّد** (`waste-management/common/dto/pagination.dto.ts`):
- `page` و`limit` **من نوع رقم موجب** — `@Type(() => Number)` يحوّل نصّ الاستعلام إلى رقم، ثم `@IsInt()` + `@Min(1)` + `@Max(50)` — فلا يمرّ نصّ ولا صفر ولا سالب ولا قيمة مبالغ فيها.
- **عقد استجابة موحّد** عبر `buildPagination()`: `{current_page, total_pages, total_count, limit, has_next, has_prev}` محسوبة على نفس مجموعة التصفية (عبر `getManyAndCount`) فلا يتناقض العدّاد مع الصفحة.
- **تغطية شاملة:** حتى الرواتات ذات المعامل المفرد (مثل تاريخ تتبّع الشاحنة) تستخدم DTO مُتحقَّقاً بدل قراءة نصّ خام (`truck-tracking.controller.ts`).

---

## 10) بيئة التحقّق والإعدادات القابلة للضبط

| المكوّن | التفصيل |
|---|---|
| Node.js | v20.11.1 · NestJS 11 · TS 5.7 |
| قاعدة البيانات | PostgreSQL 16 (`dawrha_db`) · TypeORM · `synchronize:false` · 91 هجرة |
| الكاش/الطوابير/الحدّ | Redis 7 |
| الأودو | Odoo 19 (حاوية Docker) · addon `recycle_warehouse` |
| أداة الحِمل | k6 v2.2.0 |

**مفاتيح الضبط (`.env` + Joi):** الحدّ `100/60s/IP`؛ JWT (3 أسرار + 4 مدد صلاحية)؛ TTL الكاش لكل مورد؛ تدوير السجلّات؛ تكاملات Cloudinary/Firebase/SMTP/Odoo؛ سرّ webhook الأودو (≥32 حرفاً وإلا تُعطَّل المنافذ).

---

## 11) ملخّص الحالة (لكل متطلّب)
| المتطلّب غير الوظيفي | الحالة |
|---|---|
| الأمن — مصادقة/تفويض/تحقّق مدخلات/حدّ معدّل/أسرار/webhooks موقّعة | ✅ مطبَّق بقوّة |
| الأمن — Helmet / CORS | ✅ مطبَّق ومُثبَت حيّاً (§1.7) |
| الأداء — كاش (بحارس فارغ)/فهرسة/ترقيم مُتحقَّق/تصفية SQL/طوابير | ✅ مطبَّق ومقيس |
| قابلية التوسّع — Stateless/Redis مشترك/Worker مستقل | ✅ مطبَّق |
| الموثوقية — استثناءات/معاملات/idempotency/طوابير/**مصالحة Odoo** | ✅ مطبَّق |
| قابلية الصيانة — وحدات/944+8 اختبار/91 هجرة | ✅ مطبَّق |
| التدويل i18n (ar/en) — **لغة لكل حساب** + فرض تغطية | ✅ مطبَّق (متقدّم) |
| المراقبة — سجلّات دوّارة/Morgan/تدقيق | ✅ مطبَّق |
| تصميم API — بادئة + إصدارات URI + عقد موحّد | ✅ مطبَّق |
