/**
 * Supported UI / notification languages. Values match the i18n locale folders
 * (src/i18n/<lang>) so they can be passed straight to I18nService.translate().
 */
export enum Language {
  EN = 'en',
  AR = 'ar',
}
